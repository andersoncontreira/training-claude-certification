# Local Overrides

<!-- This file is personal and must be listed in .gitignore -->

- My local Python interpreter: `~/.pyenv/versions/3.12.3/bin/python`
- Local DB connection string is in `.env.local` — never commit this file.
- I prefer shorter explanations; skip boilerplate preamble in responses.
- When running DB seeds, always confirm before executing against a non-local DB.
